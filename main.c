#include <kipr/wombat.h>

int main()
{
    printf("Hello World\n");\
        camera_open_black();
    camera_update();
    
    
    while (get_object_count(==0);
    {
        motor(0,50);
        motor(3,50);
    }
    
    while (get_object_count(==1);
    {
        motor(0,0);
        motor(3,0);
    }
    return 0;
}
